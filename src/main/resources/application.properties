spring.jpa.database=POSTGRESQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/order_manager
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:admin}

email.from.email=${EMAIL_FROM_EMAIL:testprograming@outlook.com}
email.from.password=${EMAIL_FROM_PASSWORD:Tijolo+-}
email.smtp.auth=${EMAIL_SMTP_AUTH:true}
email.smtp.starttls.enable=${EMAIL_SMTP_TTL:true}
email.smtp.host=${EMAIL_SMTP_HOST:smtp-mail.outlook.com}
mail.smtp.port=${EMAIL_SMTP_PORT:587}
